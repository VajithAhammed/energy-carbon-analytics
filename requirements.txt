# Energy Consumption & Carbon Analytics -- dependencies
# Pinned to the exact versions this project was built and validated
# against. Newer versions will likely work; these are the ones every
# number in this repo was actually produced with.

pandas==3.0.2
numpy==2.4.4
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
joblib==1.5.3

# To open and run the notebooks:
jupyter>=1.0.0
notebook>=7.0.0

# Optional -- only needed if you set up a live PostgreSQL instance
# (see reports/postgres_setup.md):
psycopg2-binary>=2.9.0
